# Минимальный набор для eval harness.
# Базовые автоматические метрики работают без всего этого
# (loop detector, kz_purity, latency — чистый stdlib).

# Лексические метрики
sacrebleu>=2.4.0
rouge-score>=0.1.2

# Семантическое сходство (BERTScore) — опционально, тяжёлое
bert-score>=0.3.13   # потянет torch + transformers ~2GB

# LLM-judge (один из двух, не обязательно оба)
openai>=1.55.0
anthropic>=0.40.0    # опционально, если хотите Claude вместо GPT-4

# Для подгрузки конфига из yaml (опционально, в коде fallback на JSON)
pyyaml>=6.0
